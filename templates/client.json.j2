{
   "client": {
      "name": "{{ sensu_client_hostname }}",
      "address": "{{ sensu_client_address }}",
      "subscriptions": [ "{{ "\",\"".join(sensu_client_subscription_names) }}" ]{% if sensu_settings %},
      {% for k, v in sensu_settings.iteritems() %}
      "{{k}}": {{v|to_json}}{% if not loop.last %},{%endif%}
      {% endfor %}
      {% endif %}
   }
}
